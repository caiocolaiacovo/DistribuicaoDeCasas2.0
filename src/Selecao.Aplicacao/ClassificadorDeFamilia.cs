using System;
using System.Collections.Generic;
using Selecao.Dominio;

namespace Selecao.Aplicacao
{
    public class ClassificadorDeFamilia
    {
        // private ICriterioRepositorio _criterioRepositorio;

        // public ClassificadorDeFamilia(ICriterioRepositorio criterioRepositorio)
        // {
        //     _criterioRepositorio = criterioRepositorio;
        // }
        
        // public void Classificar(Familia familia)
        // {
        //     var criterios = _criterioRepositorio.ObterTodos();

        //     foreach (var criterio in criterios)
        //     {
        //         if (criterio.Satisfaz(familia))
        //             familia.AdicionarCriterio(criterio);
        //     }
        // }
    }
}